import Blockquote from '@tiptap/extension-blockquote';

// Re-export Tiptap's Blockquote extension
// This is already included in StarterKit but we export it explicitly
// for clarity and potential customization
export const Quote = Blockquote;
