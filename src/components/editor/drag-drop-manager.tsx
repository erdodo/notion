'use client';

interface DragDropManagerProperties {
  _editor: any;
  _documentId?: string;
}

export const DragDropManager = ({
  _editor,
  _documentId,
}: DragDropManagerProperties) => {
  return null;
};
